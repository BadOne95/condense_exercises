import { PrismaClient } from "@prisma/client";

export const getAllShoesFromPath = async (path: string) => {
  // effettua una query sulla tabella shoe_to_category
  const client = new PrismaClient();
  client;
  return Promise.resolve();
};
